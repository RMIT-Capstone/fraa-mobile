import React from 'react';
import { View, Text, TextInput, StyleSheet } from 'react-native';

const ChangePassword = ({ navigation, email, setEmail }) => {
  return (
    <View>
      <Text>This is change password</Text>
    </View>
  )
};

export default ChangePassword;
